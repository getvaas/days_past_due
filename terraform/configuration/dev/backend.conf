# Backend del state de Terraform (S3 + lock DynamoDB).
# CONFIRMAR valores: reusa el bucket/lock de state de dev con una key propia de DPD.
bucket         = "6fc5w786-dev-terraform-states-bucket"
key            = "days-past-due/terraform.tfstate"
region         = "us-east-1"
dynamodb_table = "dev-terraform-lock-table"
encrypt        = true
