# Core
PyMySQL>=1.1
pandas>=2.0
polars>=0.20
python-dateutil>=2.8

# Lambda / AWS
boto3>=1.34
pyarrow>=14.0       # parquet support en s3_io
requests>=2.28      # clients HTTP (Company Provider / M2M)

# Local testing / notebooks
openpyxl>=3.1       # excel_runner export
